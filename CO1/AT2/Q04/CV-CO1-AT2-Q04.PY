# -*- coding: utf-8 -*-
"""CV-CO1-AT2-Q04

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1X33kLWWc1Wwa0nM7ndn1vC0c4HDQ8W2n
"""

# ==========================================
# Satellite Image Processing for Land Analysis
# Google Colab Code
# ==========================================

import cv2
import numpy as np
import matplotlib.pyplot as plt
from google.colab import files

# Upload a satellite image
print("Upload a satellite image")
uploaded = files.upload()

# Read the uploaded image
image_path = list(uploaded.keys())[0]
image = cv2.imread(image_path)

# Convert BGR to RGB
image_rgb = cv2.cvtColor(image, cv2.COLOR_BGR2RGB)

# Convert to Grayscale
gray = cv2.cvtColor(image, cv2.COLOR_BGR2GRAY)

# Histogram Equalization (Image Enhancement)
enhanced = cv2.equalizeHist(gray)

# Gaussian Blur (Noise Reduction)
blur = cv2.GaussianBlur(enhanced, (5,5), 0)

# Edge Detection using Canny
edges = cv2.Canny(blur, 100, 200)

# Display Results
titles = ['Original Image', 'Grayscale Image',
          'Enhanced Image', 'Edge Detection']

images = [image_rgb, gray, enhanced, edges]

plt.figure(figsize=(12,8))

for i in range(4):
    plt.subplot(2,2,i+1)
    if len(images[i].shape)==2:
        plt.imshow(images[i], cmap='gray')
    else:
        plt.imshow(images[i])
    plt.title(titles[i])
    plt.axis('off')

plt.tight_layout()
plt.show()

print("Processing Completed Successfully!")
